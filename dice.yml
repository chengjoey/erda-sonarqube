version: '2.0'
values:
services:
  sonar:
    ports:
      - port: 9000
        expose: true
    envs:
      SONAR_ADMIN_PASSWORD: ${env.SONAR_ADMIN_PASSWORD}
      SONAR_ADMIN_TOKEN: ${env.SONAR_ADMIN_TOKEN}
    resources:
      cpu: 0.5
      mem: 1024
    deployments:
      replicas: 1